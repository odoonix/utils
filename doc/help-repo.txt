The odoo utils is a basic tool to prepare both production and development env.


Usage: repo [-h]


     --help,-h: Display help
   --check, -c: Checks all repos
    --init, -i: Init the repo if not exist and update use -i all for all repo
     --console: Enable VW developer console


